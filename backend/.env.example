DATABASE_URL="postgresql://postgres:postgres@localhost:5432/project_tracking?schema=public"
# Generate real random values, e.g.: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="change-me-in-production"
JWT_REFRESH_SECRET="change-me-too"
PORT=3001
# Comma-separated list of frontend origins allowed to call this API.
CORS_ORIGIN="http://localhost:5173"
